$schema: https://json-schema.org/draft/2020-12/schema
title: Print Profile
description: Print profile for digital letters
type: object
allOf:
  - $ref: ./digital-letters-profile.schema.json
properties:
  type:
    type: string
    minLength: 1
    allOf:
      - name: NHS Notify versioned event type pattern
        type: string
        pattern: ^uk\.nhs\.notify\.digital\.letters\.print\.[a-z0-9]+(?:\.[a-z0-9]+)*\.v[0-9]+$
  source:
    type: string
    pattern: ^/nhs/england/notify/(production|staging|development|uat)/(primary|secondary|dev-[0-9]+)/digitalletters/print
    minLength: 30
    format: uri-reference
