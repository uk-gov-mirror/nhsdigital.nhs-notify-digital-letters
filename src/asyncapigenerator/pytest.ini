[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=.
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-config=pytest.ini
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests

[coverage:run]
relative_files = True
data_file = src/asyncapigenerator/.coverage
omit =
    */tests/*
    */test_*.py
    src/asyncapigenerator/test_generator.py
    src/asyncapigenerator/example_usage.py
    test_generator.py
    example_usage.py
    */venv/*
    */.venv/*

[coverage:xml]
output = coverage.xml
